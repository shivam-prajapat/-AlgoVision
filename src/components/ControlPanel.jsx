import { useState } from 'react';import { motion } from 'framer-motion';export default function ControlPanel({  onStart,  onPause,  onResume,  onReset,  onStep,  onSpeedChange,  onGenerateData,  onTargetChange,  onSizeChange,  isRunning,  isPaused,  isComplete,  speed,  target,  dataSize,  algorithm,}) {  const isGraphAlgo = algorithm === 'dfs' || algorithm === 'bfs';  const [inputTarget, setInputTarget] = useState(target || '');  const handleTargetInput = (e) => {    setInputTarget(e.target.value);    onTargetChange?.(e.target.value);  };  const speedLabel = speed <= 100 ? '4×' : speed <= 200 ? '3×' : speed <= 350 ? '2×' : speed <= 500 ? '1×' : speed <= 750 ? '0.5×' : '0.25×';  return (    <motion.div      className="glass rounded-2xl p-4"      initial={{ y: 40, opacity: 0 }}      animate={{ y: 0, opacity: 1 }}      transition={{ delay: 2.6, duration: 0.5, ease: 'easeOut' }}    >      <div className="flex flex-wrap items-center gap-3 justify-center lg:justify-between">        {}        <div className="flex items-center gap-2">          {}          {!isRunning || isPaused ? (            <motion.button              onClick={isRunning && isPaused ? onResume : onStart}              disabled={isComplete && !isPaused}              className="h-10 px-5 rounded-xl font-semibold text-sm flex items-center gap-2 cursor-pointer border-0 disabled:opacity-40 disabled:cursor-not-allowed"              style={{                background: 'linear-gradient(135deg, rgba(0,255,136,0.2), rgba(0,212,255,0.2))',                border: '1px solid rgba(0,255,136,0.3)',                color: '#00ff88',                boxShadow: '0 0 15px rgba(0,255,136,0.15)',              }}              whileHover={{ scale: 1.05, boxShadow: '0 0 25px rgba(0,255,136,0.3)' }}              whileTap={{ scale: 0.95 }}            >              <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">                <path d="M8 5v14l11-7z" />              </svg>              {isRunning && isPaused ? 'Resume' : 'Start'}            </motion.button>          ) : (            <motion.button              onClick={onPause}              className="h-10 px-5 rounded-xl font-semibold text-sm flex items-center gap-2 cursor-pointer border-0"              style={{                background: 'rgba(255, 215, 0, 0.15)',                border: '1px solid rgba(255, 215, 0, 0.3)',                color: '#ffd700',              }}              whileHover={{ scale: 1.05 }}              whileTap={{ scale: 0.95 }}            >              <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">                <path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z" />              </svg>              Pause            </motion.button>          )}          {}          <motion.button            onClick={onStep}            disabled={isComplete}            className="h-10 px-4 rounded-xl font-semibold text-sm flex items-center gap-2 cursor-pointer border-0 disabled:opacity-40 disabled:cursor-not-allowed"            style={{              background: 'rgba(0, 212, 255, 0.12)',              border: '1px solid rgba(0, 212, 255, 0.25)',              color: '#00d4ff',            }}            whileHover={{ scale: 1.05 }}            whileTap={{ scale: 0.95 }}          >            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">              <path d="M6 18l8.5-6L6 6v12zM16 6v12h2V6h-2z" />            </svg>            Step          </motion.button>          {}          <motion.button            onClick={onReset}            className="h-10 px-4 rounded-xl font-semibold text-sm flex items-center gap-2 cursor-pointer border-0"            style={{              background: 'rgba(255, 0, 110, 0.12)',              border: '1px solid rgba(255, 0, 110, 0.25)',              color: '#ff006e',            }}            whileHover={{ scale: 1.05 }}            whileTap={{ scale: 0.95 }}          >            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">              <path d="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z" />            </svg>            Reset          </motion.button>        </div>        <div className="hidden lg:block w-px h-8" style={{ background: 'rgba(255,255,255,0.08)' }} />        <div className="flex items-center gap-3">          <span className="text-[11px] font-medium" style={{ color: '#64748b' }}>            Speed          </span>          <div className="flex items-center gap-2">            <span className="text-xs font-mono" style={{ color: '#475569' }}>Slow</span>            <input              type="range"              min="50"              max="1000"              step="50"              value={1050 - speed}              onChange={(e) => onSpeedChange(1050 - parseInt(e.target.value))}              className="w-24 h-1.5 rounded-full appearance-none cursor-pointer"              style={{                background: `linear-gradient(to right, #00d4ff ${((1050 - speed - 50) / 950) * 100}%, rgba(255,255,255,0.1) ${((1050 - speed - 50) / 950) * 100}%)`,                accentColor: '#00d4ff',              }}            />            <span className="text-xs font-mono" style={{ color: '#475569' }}>Fast</span>          </div>          <span            className="text-xs font-mono px-2 py-0.5 rounded"            style={{ background: 'rgba(0,212,255,0.1)', color: '#00d4ff' }}          >            {speedLabel}          </span>        </div>        <div className="hidden lg:block w-px h-8" style={{ background: 'rgba(255,255,255,0.08)' }} />        <div className="flex items-center gap-3">          {!isGraphAlgo && (            <div className="flex items-center gap-2">              <label className="text-[11px] font-medium" style={{ color: '#64748b' }}>                Target              </label>              <input                type="number"                value={inputTarget}                onChange={handleTargetInput}                disabled={isRunning}                placeholder="?"                className="w-16 h-8 px-2 rounded-lg text-sm font-mono text-center border-0 outline-none disabled:opacity-50"                style={{                  background: 'rgba(255,255,255,0.05)',                  border: '1px solid rgba(255,255,255,0.1)',                  color: '#e2e8f0',                }}              />            </div>          )}          {!isGraphAlgo && (            <div className="flex items-center gap-2">              <label className="text-[11px] font-medium" style={{ color: '#64748b' }}>                Size              </label>              <input                type="range"                min="5"                max="40"                value={dataSize}                onChange={(e) => onSizeChange(parseInt(e.target.value))}                disabled={isRunning}                className="w-16 h-1.5 rounded-full appearance-none cursor-pointer"                style={{                  background: `linear-gradient(to right, #a855f7 ${((dataSize - 5) / 35) * 100}%, rgba(255,255,255,0.1) ${((dataSize - 5) / 35) * 100}%)`,                  accentColor: '#a855f7',                }}              />              <span className="text-xs font-mono" style={{ color: '#a855f7' }}>                {dataSize}              </span>            </div>          )}          <motion.button            onClick={onGenerateData}            disabled={isRunning}            className="h-8 px-4 rounded-lg font-medium text-xs flex items-center gap-1.5 cursor-pointer border-0 disabled:opacity-40 disabled:cursor-not-allowed"            style={{              background: 'rgba(168, 85, 247, 0.15)',              border: '1px solid rgba(168, 85, 247, 0.25)',              color: '#a855f7',            }}            whileHover={{ scale: 1.05 }}            whileTap={{ scale: 0.95 }}          >            <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor">              <path d="M19 8l-4 4h3c0 3.31-2.69 6-6 6-1.01 0-1.97-.25-2.8-.7l-1.46 1.46C8.97 19.54 10.43 20 12 20c4.42 0 8-3.58 8-8h3l-4-4zM6 12c0-3.31 2.69-6 6-6 1.01 0 1.97.25 2.8.7l1.46-1.46C15.03 4.46 13.57 4 12 4c-4.42 0-8 3.58-8 8H1l4 4 4-4H6z" />            </svg>            Randomize          </motion.button>        </div>      </div>    </motion.div>  );}